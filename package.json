{
  "name": "hyperdesk-scout",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Hyperliquid funding intelligence service for OKX AI A2MCP",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "NODE_ENV=test node --test",
    "check": "node --check src/*.js public/app.js && node -e \"const fs=require('node:fs'); JSON.parse(fs.readFileSync('openapi.json','utf8')); JSON.parse(fs.readFileSync('.impeccable/design.json','utf8'))\""
  },
  "engines": {
    "node": ">=20"
  }
}
