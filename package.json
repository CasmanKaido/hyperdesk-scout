{
  "name": "hyperdesk-scout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hyperliquid funding intelligence service for OKX AI A2MCP",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "NODE_ENV=test node --test",
    "check": "node --check src/*.js && node -e \"JSON.parse(require('node:fs').readFileSync('openapi.json', 'utf8'))\""
  },
  "engines": {
    "node": ">=20"
  }
}
