{
  "name": "hyperdesk-scout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hyperliquid funding intelligence service for OKX AI A2MCP",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "NODE_ENV=test node --test",
    "check": "node --check src/server.js && node --check src/hyperliquid.js && node --check src/scoring.js"
  },
  "engines": {
    "node": ">=20"
  }
}
